# Auth System

## Goal
Initialize the backend and create an account system where users can sign in or register with an email address and password. Each user must have their own GUID userID.

## Requirements
- Backend initialization/setup
- User registration with email + password
- User sign-in with email + password
- Each user has a unique GUID as their userID
